// A little bit of ES6 import (badly supported except for webpack)
// import { Map } from "immutable";

console.log('using webpack and babel no immutable');

// A little bit of ES6 to prove babel right
const a = [1,2,3,4,5];
a.map(v => console.log(v));


